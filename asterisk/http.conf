; Asterisk 内蔵 HTTP サーバー (SIP over WebSocket 用)
; ブラウザは FastAPI の /ws に wss で接続し、FastAPI が ws://127.0.0.1:8088/ws へ中継する。
; そのため 127.0.0.1 にだけ bind し、外から直接は触れないようにする。
[general]
enabled=yes
bindaddr=127.0.0.1
bindport=8088
